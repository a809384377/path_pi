## Review
- **PASS — no Blocker/Major findings.** HEAD is `dfa7438e66eebaacefd8251df00ed21429f8dc37`.
- **Correct:** Shutdown uses `Promise.allSettled` before aggregating failures, so server exit cannot precede another session’s cleanup completion (`src/session/session-manager.ts:304`, `src/server.ts:113`); regression coverage is at `test/session-manager.integration.test.ts:495`.
- **Correct:** Persistence failure immediately rejects existing and subsequent waits with `persistence_error`, while shutdown still reaches process cleanup and preserves a dirty manifest (`src/session/session-manager.ts:225`, `src/session/session-manager.ts:487`, `src/session/session-manager.ts:573`); real-process coverage is at `test/session-manager.integration.test.ts:426`.
- **Correct:** Terminal publication preserves lifecycle ownership during close, and cleanup only clears its captured task generation, preventing reopening and ghost tasks (`src/session/session-manager.ts:501`, `src/session/session-manager.ts:570`, `src/session/session-manager.ts:608`); covered at `test/session-manager.integration.test.ts:455` and `test/session-manager.integration.test.ts:533`.
- **Correct:** POSIX ownership retains the PGID after leader exit, waits for live non-zombie members, detects an observed reused leader PID before group signaling, and escalates TERM-resistant descendants to SIGKILL (`src/rpc/pi-rpc-process.ts:52`, `src/rpc/pi-rpc-process.ts:326`, `src/rpc/pi-rpc-process.ts:357`); real stubborn-descendant coverage exists at `test/session-manager.integration.test.ts:426` and `test/stdio-shutdown.e2e.test.ts:29`.
- **Correct:** Exactly five tools remain registered and exercised end-to-end (`src/server.ts:33`, `src/server.ts:56`, `src/server.ts:69`, `src/server.ts:84`, `src/server.ts:97`, `test/mcp-tools.integration.test.ts:25`).
- **Note:** No commands were executed because the reviewer role prohibits shell/Git commands. Supervisor should run `npm run typecheck && npm test` for runtime attestation. Residual risks are limited to spec-excluded abrupt host `SIGKILL`/machine failure and the inherent userspace race between inspecting and signaling a POSIX process group.
