{
  "name": "pi-agent-mcp-development",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:package": "npm --prefix ../npm run build",
    "typecheck": "npm run build:package && tsc -p tsconfig.json --noEmit",
    "test": "npm run build:package && node --import tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "24.5.2",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}
